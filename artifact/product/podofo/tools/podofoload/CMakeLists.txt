ADD_EXECUTABLE(podofoload podofoload.cpp)

TARGET_LINK_LIBRARIES(podofoload ${PODOFO_LIB})
SET_TARGET_PROPERTIES(podofoload PROPERTIES COMPILE_FLAGS "${PODOFO_CFLAGS}")
ADD_DEPENDENCIES(podofoload ${PODOFO_DEPEND_TARGET})
INSTALL(TARGETS podofoload
	RUNTIME DESTINATION "bin")
